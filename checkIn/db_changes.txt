//DB CHANGE LOG:

// safetyTraining "date" changed to "in_person_date"
// SQL:
alter table safetyTraining change date in_person_date datetime not null;

// safetyTraining new column "videos_watched"
// SQL:
alter table safetyTraining
    add videos_watched varchar(100) null;

// Model.py "date" changed to "in_person_date" and made nullable
// Model.py "trainer_id" made nullable

// Model.py new class video (tablename is video)
// video contains: id, length, filepath, name, descrip

// new table video w/ columns: id primaryKey int, length int, filepath text, name varchar(100), descrip varchar(100)
// SQL:
create table video
(
	id int not null,
	length int not null,
	filepath text not null,
	name varchar(100) null,
	descrip text null,
	constraint video_pk
		primary key (id)
);

